import { describe, it, expect } from 'vitest';
import reducer, { addList, deleteList, clearBoard, deleteCard, addCard } from '../components/slices/listsSlice';


describe('listsSlice', () => {
  const initialState = {
    lists: [],
    cards: {},
  };

  it('Should handle addList', () => {
    const action
  })
})
